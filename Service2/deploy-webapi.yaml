apiVersion: apps/v1
kind: Deployment
metadata:
  name: service2
  labels:
    app: weather-forecast
spec:
  replicas: 1
  selector:
    matchLabels:
      service: service2
  template:
    metadata:
      labels:
        app: weather-forecast
        service: service2
    spec:
      containers:
        - name: service2
          image: exploredockerrahul.azurecr.io/service:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              protocol: TCP
      imagePullSecrets:
        - name: acr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: service2
  labels:
    app: weather-forecast
    service: service2
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    service: service2